---
title: "Fix Keyly Issues - Troubleshooting & Solutions Guide"
description: "Shortcuts not showing? Permissions denied? Menu bar icon missing? Follow our step-by-step troubleshooting guide to resolve common Keyly issues fast."
---

import { Steps, Aside } from '@astrojs/starlight/components';

## Keyly Not Showing Shortcuts

### Check Accessibility Permissions

<Steps>
1. Open **System Settings**
2. Go to **Privacy & Security → Accessibility**
3. Make sure **Keyly** is in the list and **enabled**
4. If not listed, try launching Keyly first
</Steps>

<Aside type="caution">
After enabling permissions, you may need to restart Keyly for changes to take effect.
</Aside>

### Restart Keyly

<Steps>
1. Click the **⌘ icon** in the menu bar
2. Select **Quit**
3. Relaunch Keyly from Applications
</Steps>

## Shortcuts Window Not Appearing

### Hold ⌘ Long Enough

You need to hold the Command key for **2 full seconds**. A quick press won't trigger the overlay.

### Check If App Has Menu Bar

Some apps (especially games or full-screen apps) may not expose their shortcuts via the menu bar. Keyly can only read what's accessible through the Accessibility API.

## Some Shortcuts Are Missing

### Not All Shortcuts Can Be Detected

This is a macOS limitation, not a bug. Some shortcuts are **internal** — the app handles them directly without going through macOS.

For example, VS Code's `⌘P` (Quick Open) or `⌘⇧P` (Command Palette) are internal shortcuts that macOS can't see.

<Aside>
Learn more about this in [Why Some Shortcuts Can't Be Detected](/resources/detection-limits/).
</Aside>

### Solution: Use Custom Configurations

You can manually add any missing shortcuts:

1. **Use a template** — Check [Templates](/configuration/templates/) for pre-made configs
2. **Create your own** — See [Custom Shortcuts](/configuration/custom-shortcuts/)

## Custom Shortcuts Not Working

### Verify File Location

Make sure your `.keyly` file is in:
```
~/.config/keyly/
```

### Check App Path

The `# App:` header must point to the correct application bundle:

```
# App: /Applications/Safari.app
```

Find the correct path:
```bash
ls /Applications/*.app
```

## Template Not Working After Installation

<Aside type="caution">
Templates from the community use default app paths that may not match your system.
</Aside>

### App Path Mismatch

The most common reason a template doesn't work is that the **app path is different** on your Mac. This happens because:

- **Different installation methods** — App Store apps vs direct downloads may have different bundle names
- **Version variations** — Some apps include version info in their name (e.g., `Cursor - Nightly.app`)
- **Custom locations** — Apps installed in `~/Applications/` instead of `/Applications/`

### How to Fix

<Steps>
1. Open the template file in `~/.config/keyly/`
2. Find the `# App:` line at the top of the file
3. Update it to match your app's actual path
</Steps>

Find your app's correct path:
```bash
# List apps in /Applications
ls /Applications/*.app

# List apps in ~/Applications
ls ~/Applications/*.app
```

Example:
```keyly
# Template default (might not work)
# App: /Applications/Cursor.app

# Your actual path (update to this)
# App: /Applications/Cursor - Nightly.app
```

After updating, reload Keyly: **⌘ icon → Reload**

### Reload Keyly

After editing config files, reload from the menu bar: **⌘ icon → Reload**

### Check File Encoding

Make sure your `.keyly` file is saved as **UTF-8** without BOM.

## Menu Bar Icon Missing

### Check Menu Bar Space

If you have many menu bar items, Keyly's icon might be hidden. Try:
- Holding ⌘ and dragging icons to rearrange
- Using an app like Bartender or Hidden Bar to manage menu bar items

### Verify Keyly Is Running

Open Activity Monitor and search for "Keyly". If not running, launch it from Applications.

## App Crashes or Freezes

### Check macOS Version

Keyly requires **macOS 12.0 or later**. Check your version:
1. Click  → About This Mac
2. Look for macOS version

### Reset Keyly

Remove config and restart:
```bash
rm -rf ~/.config/keyly
```

Then relaunch Keyly.

## Still Having Issues?

1. Check the [FAQ](/faq/) for common questions
2. [Open an issue on GitHub](https://github.com/hoaiphongdev/keyly/issues)

## Related

- [Getting Started](/getting-started/) — Installation guide
- [Configuration](/configuration/) — Customize Keyly
- [FAQ](/faq/) — Common questions
- [Why Some Shortcuts Can't Be Detected](/resources/detection-limits/) — Detection limitations
